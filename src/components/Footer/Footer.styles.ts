import {StyleSheet} from 'react-native';
import {colors} from '../../theme/colors';

export const styles = StyleSheet.create({
  footer: {
    height: 64,
    backgroundColor: colors.base.white,
    borderTopWidth: 1,
    borderTopColor: colors.base.grayShadow,
  },
});
